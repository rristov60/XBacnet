import { IconButton } from "@mui/material"
import { Fingerprint } from "@mui/icons-material"

const TheIconButton = () => {
  return (
    <IconButton aria-label="fingerprint" color="secondary">
        <Fingerprint />
    </IconButton>
  )
}

export default TheIconButton